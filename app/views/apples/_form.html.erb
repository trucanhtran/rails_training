<%= form_with(model: apple) do |form| %>
  <% if apple.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apple.errors.count, "error") %> prohibited this apple from being saved:</h2>

      <ul>
        <% apple.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
