<%= form_with(model: ruler) do |form| %>
  <% if ruler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ruler.errors.count, "error") %> prohibited this ruler from being saved:</h2>

      <ul>
        <% ruler.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="field">
    <%= form.label :lenght %>
    <%= form.text_field :lenght %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
