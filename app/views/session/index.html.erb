  <% if has_login? === false %>
    <div class = "btn-block">
      <%= link_to 'Sign Up', new_user_path, class: "btn btn-primary btn-signup" %>
    </div>
    <div class = "btn-block">
      <%= link_to 'Login', login_path, class: "btn btn-primary btn-login" %>
    </div>
  <% else %>
    <% flash.each do |key,value|  %>
      <div class= "flash flash-<% key %>">
        <%= value %>
      </div>
    <% end %>
  <% end %>
