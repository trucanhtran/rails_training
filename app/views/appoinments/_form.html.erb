<%= form_with(model: appoinment) do |form| %>
  <% if appoinment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appoinment.errors.count, "error") %> prohibited this appoinment from being saved:</h2>

      <ul>
        <% appoinment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :room %>
    <%= form.number_field :room %>
  </div>

  <div class="field">
    <%= form.label :patient_id %>
    <%= form.collection_select :patient_id, patients, :id, :name, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label :physican_id %>
    <%= form.collection_select :physican_id, physicans, :id, :name, include_blank: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
